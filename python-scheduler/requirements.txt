# Core Job Runner dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Prometheus metrics
prometheus-client>=0.19.0

# Python script dependencies (for /bin scripts)
pyasic>=0.50.0
pyyaml>=6.0
netifaces>=0.11.0
aiohttp>=3.9.0
aiohttp-digest>=0.3.0

# Healthcheck
requests>=2.31.0
