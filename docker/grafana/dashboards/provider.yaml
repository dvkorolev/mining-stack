apiVersion: 1

providers:
  - name: 'default' # A name for this provider
    orgId: 1
    folder: 'Mining' # Dashboards will be grouped in a "Mining" folder in the Grafana UI
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/dashboards